export function navSideBar() {
    
    let btns = '';
    
    let navSideBar = 
    `
    <nav>
        <button>
            <i class=""></i>
        </button>
        <section>
            ${btns}
        </section>
    </nav>
    
    `;
}

function navBtn(title, iconClassName, pageLink, noTitle = false)
{

    let btn = 
    `

    `;
    return btn;
}

